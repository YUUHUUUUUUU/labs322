import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.ArrayList;

public class EffectTest {
    //testar Effect (construct, getters, subscribe,reduceDuration), PoisonEffect/EnergyEffect/ShieldEffect (Construct,Use,Copy,Usedescription), CardEffect(Construct,Use).
}
